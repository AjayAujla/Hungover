using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class SoundController : MonoBehaviour {

    private Slider slider;
    private Toggle check;

    /*
    TODO: Configure audio controls
    void Start()
    {
        this.slider = this.gameObject.GetComponentInChildren<Slider>();
        this.check = this.gameObject.GetComponentInChildren<Toggle>();
    }

    void Update()
    {
        if(!this.check.isOn)
        {
            this.slider.enabled = false;
        }

        if(this.slider.value == 0)
        {
            this.check.isOn = false;
        }
    }

    void toggleSliderActive()
    {

    }
    */
}